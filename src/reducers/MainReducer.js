import {
   
} from '../actions/types'

const INITIAL_STATE = {
    filled : false,
    role : '',
    blood : '',
    factor : '',
    pload : false,
}

export default (state = INITIAL_STATE, action) => {
    switch(action.type) {

    default:
            return state
    }
}